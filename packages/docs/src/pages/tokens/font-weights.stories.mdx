import { Meta } from '@storybook/addon-docs'
import { TokensGrid } from '../../components/TokensGrid'
import { fontWeights } from '@munizeduardo-ignite-ui/tokens'

<Meta title="Tokens/Font Weigths" />

# Font Weigths

These are the font weigths utilized in the Ignite UI.

<TokensGrid tokens={fontWeights} />
